<p>THIS IS OUR AWSOME FORM</p>

<%= form_for(@post) do |p| %>

<% if @post.errors.any? %>
   <div id="error_explanation">
     <h2><%= pluralize(@post.errors.count, "error") %> prohibited this url    from being saved:</h2>

        <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
   </div>
 <% end %>

  <div class="field">
    <%= p.label :title %> <br />
    <%= p.text_field :title %>

  </div>

  <div class="field">
    <%= p.label :url %> <br />
    <%= p.text_field :url %>
  </div>

  <div class="actions">
    <%= p.submit %>
  </div>

  <% end %>
